<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Always-dark theme for image_cropper's UCropActivity (avatar square-crop UI).
         Inherits from non-Light AppCompat so toolbar action items render reliably
         across OEM skins; fitsSystemWindows pushes content below status bar and
         above gesture nav so the ✓/✕ icons aren't clipped. -->
    <style name="UCropTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">#0E1525</item>
        <item name="colorPrimaryDark">#0E1525</item>
        <item name="colorAccent">#00C2D1</item>
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:windowBackground">#FF000000</item>
        <item name="android:navigationBarColor">#FF000000</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>
</resources>
