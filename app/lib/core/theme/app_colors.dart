import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette
  static const electricAqua = Color(0xFF00C2D1);
  static const deepBlue = Color(0xFF2563EB);
  static const violetAccent = Color(0xFF7C3AED);

  // Backgrounds
  static const softAquaBackground = Color(0xFFE6FBFF);
  static const whiteSurface = Color(0xFFFFFFFF);

  // Text
  static const primaryText = Color(0xFF0F172A);
  static const secondaryText = Color(0xFF64748B);

  // Semantic
  static const error = Color(0xFFDC2626);
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);

  // Gradient colors
  static const gradientStart = electricAqua;
  static const gradientMiddle = deepBlue;
  static const gradientEnd = violetAccent;
}
